name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test dependencies
        run: python -m pip install -r requirements-dev.txt
      - name: Verify Python runtime dependencies
        shell: pwsh
        run: |
          python -c "import importlib.metadata as metadata; import jsonschema; from PIL import Image; print('jsonschema=' + metadata.version('jsonschema')); print('Pillow=' + metadata.version('Pillow'))"
      - name: Install FFmpeg for integration tests
        run: choco install ffmpeg --yes --no-progress
      - name: Verify FFmpeg tools
        shell: pwsh
        run: |
          ffmpeg -version
          ffprobe -version
      - name: Compile Python
        run: python -m compileall skills/reference-video-rebuilder/scripts tests
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Validate example Template IR
        run: python skills/reference-video-rebuilder/scripts/video_remix.py validate-template skills/reference-video-rebuilder/assets/project-template/template.ir.example.json --json
      - name: Validate example Compiler Plan
        run: python skills/reference-video-rebuilder/scripts/video_remix.py validate-compiler-plan skills/reference-video-rebuilder/assets/project-template/compiler.plan.example.json --json
      - name: Validate example asset manifest
        run: python skills/reference-video-rebuilder/scripts/video_remix.py validate-assets skills/reference-video-rebuilder/assets/project-template/template.ir.example.json skills/reference-video-rebuilder/assets/project-template/assets.example.json --allow-missing-files --json
