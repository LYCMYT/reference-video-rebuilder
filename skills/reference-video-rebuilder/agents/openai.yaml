interface:
  display_name: "reference-video-rebuilder"
  short_description: "Review and render authorized static S1 template rebuilds"
  default_prompt: "Use $reference-video-rebuilder to prepare and review an authorized static S1 reference-video template, enforce its motion/audio requirements, optionally run the explicitly approved image controller for still assets, then assemble, freeze, render, and QA it."
